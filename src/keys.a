WaitForKey
         bit   STROBE
-        lda   KEY
         bpl   -
         bit   STROBE
         cmp   #"*"
         bne   +
         jsr   TEXT
         jsr   PrintByID
         !byte s_reset
         jmp   $FF59
+        cmp   #$E1
         bcc   +
         and   #$DF       ; force uppercase
+        rts
